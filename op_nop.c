#include "monty.h"

/**
 * op_nop - Just kidding(?), does not do anything
 * @stack: entry point into the stack
 * @line_number: opcode line number
 * Return: Nothing
 */
void op_nop(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);
}
